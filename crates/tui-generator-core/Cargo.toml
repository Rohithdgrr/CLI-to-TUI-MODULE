[package]
name = "tui-generator-core"
description = "Framework-independent core schema and types for TUI generator"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
